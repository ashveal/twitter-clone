export type SiteConfig = typeof siteConfig

export const siteConfig = {
  name: 'Twitter Clone',
  description: 'Twitter Clone using Supabase auth',
  mainNav: [
    {
      title: 'Home',
      href: '/',
    },
  ],
}
